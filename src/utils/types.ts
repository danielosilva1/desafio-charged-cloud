export type userDetails = {
    email: string;
    name: string;
};

export type addressDetails = {
    cep: string;
    street: string;
    neighborhood: string;
    number: number;
    additionalInfo: string;
    city: string;
    state: string;
}